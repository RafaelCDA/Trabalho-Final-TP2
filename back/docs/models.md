# Modelos da Aplicação

A camada de modelos define as estruturas de dados persistidas no banco,
utilizando SQLAlchemy para mapear cada entidade em tabela e campos.

Os modelos são utilizados pelas camadas de repositórios, serviços e rotas,
servindo como base para operações de leitura e escrita no sistema.

---

## Modelo: User

Abaixo está a documentação gerada automaticamente a partir do código-fonte:

::: src.models.user
