# Camada de Repositórios

A camada de repositórios centraliza as operações de acesso e manipulação
dos dados persistidos. Cada repositório encapsula consultas e comandos
específicos de uma entidade, mantendo a lógica de persistência isolada
das demais camadas da aplicação.

---

## Repositório de Usuários

::: src.repositories.user_repository
